/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // TODO uncomment the following when firebase is updated to v9
  // experimental: {
  //   runtime: 'experimental-edge'
  // }
};

module.exports = nextConfig;
